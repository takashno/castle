server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/takashno/castle.git
          searchPaths: modules/frontend-api/configserver-repo
          basedir: build/config
